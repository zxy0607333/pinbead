# Pinbead 任务列表

最后更新：2026-05-14

## 状态说明

- `[x]`：已完成。
- `[~]`：进行中。
- `[ ]`：未开始。

## 当前产品结论

Pinbead 的方向调整为：

```text
拼豆图纸编辑器 + 可打印拼豆图库 + 图片导入转换工具
```

图片转拼豆图只生成草稿，不再作为最终成品。真正核心是 `/editor`，用户可以从零绘制，也可以导入图片草稿后继续修眼睛、轮廓、颜色和细节。

推荐 URL：

```text
/                              首页
/editor                        拼豆图纸编辑器
/convert                       图片转拼豆图纸
/admin                         管理员后台
/patterns                      免费图纸库
/categories/[categorySlug]     分类页
/pattern/[patternSlug]         单个图纸详情页
/guides                        教程列表
/guides/[guideSlug]            教程详情页
```

## 0. 已完成基础工作

### [x] 0.1 明确品牌和英文市场方向

需求：

- 品牌统一为 `Pinbead`。
- 主市场为海外英文用户。
- 不把品牌绑定到 Perler 等商标词。
- 页面文案自然覆盖 pin bead、fuse bead、bead pattern、pixel bead pattern 等搜索词。

验收标准：

- 品牌和市场方向已写入文档。
- SEO 词和品牌词边界明确。

### [x] 0.2 建立规划文档和 `.gitignore`

需求：

- 建立 `docs` 文档目录。
- 覆盖产品规划、技术栈、路线图、SEO/AdSense 和任务列表。
- 建立基础 `.gitignore`，避免提交依赖、构建产物、环境变量和本地缓存。

验收标准：

- `docs/README.md`、`docs/product-plan.md`、`docs/technical-stack.md`、`docs/roadmap.md`、`docs/content-seo-adsense.md`、`docs/task-list.md` 已存在。
- `.gitignore` 已覆盖常见 Node/Next、本地环境和构建产物。

### [x] 0.3 初始化前端项目

需求：

- 使用 Next.js App Router。
- 使用 TypeScript。
- 使用 Tailwind CSS。
- 建立 `src/app`、`src/components`、`src/lib`、`src/data`、`public` 等基础结构。
- 支持 `npm run lint` 和 `npm run build`。

验收标准：

- 本地项目可启动。
- 生产构建通过。
- 基础首页可访问。

### [x] 0.4 完成图片转换原型

需求：

- 支持本地图片上传和预览。
- 支持裁剪和尺寸设置。
- 支持基础像素化转换。
- 支持拼豆色卡匹配。
- 支持颜色数量限制。
- 支持像素图和圆珠图预览。
- 支持颜色清单和用豆数量。
- 支持 PNG 导出。

验收标准：

- 原型已经证明图片导入、色卡匹配、预览和导出链路可行。
- 这些能力后续需要迁移到 `/convert` 和 `/editor`，不继续堆在首页。

## 1. 产品方向重置

### [x] 1.1 重新确定产品定位

需求：

- 明确 Pinbead 不只是图片转换器。
- 明确 `/editor` 是核心产品。
- 明确 `/convert` 是图片导入和草稿生成入口。
- 明确 `/patterns`、`/categories`、`/pattern`、`/guides` 是 SEO 和内容承载。

验收标准：

- 所有规划文档已统一为“编辑器优先”。
- 后续任务不再围绕首页重工具继续堆功能。

### [x] 1.2 确定 URL 结构

需求：

- 使用清晰语义路径，不使用 `/c`、`/p` 这种过短路径。
- 分类页和图纸详情页分离，避免路径冲突。
- 图片转换页使用短路径 `/convert`，SEO 词放在 title、H1 和正文。

验收标准：

- URL 结构确定为：

```text
/
/editor
/convert
/admin
/patterns
/categories/[categorySlug]
/pattern/[patternSlug]
/guides
/guides/[guideSlug]
```

### [x] 1.3 确定首页设计方向

需求：

- 首页保持轻量，不承载完整编辑器。
- 首屏说明 Pinbead 是 bead pattern editor and printable pattern library。
- 主 CTA 为 `Start designing`。
- 次 CTA 为 `Convert image` 和 `Browse patterns`。
- 首页末尾可以展示精选瀑布流，但不做无限加载社区流。

验收标准：

- 首页主次明确：编辑器是核心，转换器是入口，图库是内容。
- 瀑布流只作为精选图纸展示和内链，不抢首屏主任务。

## 2. 路由和代码重构

### [x] 2.1 拆分当前首页重工具

需求：

- 从首页移除完整 `home-pattern-maker` 重工具。
- 首页只保留轻量 hero、CTA、分类入口、编辑器亮点、精选图纸、教程入口和精选瀑布流。
- 当前图片转换和导出能力迁移到 `/convert` 或 `/editor`。

验收标准：

- 首页不再包含完整上传、转换、导出流程。
- 首页移动端首屏轻量，不被工具控件挤满。
- 首页仍能引导用户进入编辑器、转换器和图库。

### [x] 2.2 新建 `/editor` 页面

需求：

- `/editor` 是核心工具页。
- 默认打开空白画布。
- 用户可以选择画布尺寸，例如 16x16、24x24、32x32、48x48、64x64，并与 `/convert` 的尺寸选项保持一致。
- 页面布局要适合长时间编辑：工具栏、画布、色卡、颜色统计清晰分区。

验收标准：

- 用户不上传图片也能开始做拼豆图。
- 编辑器页面 URL 独立可访问。
- 首页 CTA `Start designing` 指向 `/editor`。

### [x] 2.3 新建 `/convert` 页面

需求：

- `/convert` 承载图片上传、裁剪、尺寸、色卡匹配和颜色限制。
- 转换结果必须是可编辑草稿。
- 生成后提供 `Open in editor`。
- 页面文案明确说明自动转换只是 draft，需要用户继续修正。

验收标准：

- 首页 CTA `Convert image` 指向 `/convert`。
- `/convert` 可以复用现有图片转换能力。
- 转换结果能进入编辑器继续修改。

### [x] 2.4 建立共享 Pattern 模型

需求：

- 建立统一图纸模型，供编辑器、转换器、导出和图库复用。
- `cells` 存储每格颜色 ID，而不是直接存 HEX。
- 支持空白格。
- 保留版本字段，方便后续迁移。

验收标准：

- 存在 `src/lib/pattern/pattern-model.ts`。
- 图片转换输出和编辑器状态使用同一模型。
- 颜色统计、预览、导出都基于同一模型计算。

### [x] 2.5 拆分组件和工具函数

需求：

- 将当前超长 `home-pattern-maker.tsx` 拆分。
- 图片导入逻辑放入 `components/convert` 和 `lib/pattern/image-to-pattern.ts`。
- 编辑器 UI 放入 `components/editor`。
- 导出逻辑放入 `lib/pattern/pattern-export.ts`。
- 色彩匹配保留在 `lib/pattern` 或现有匹配模块中。

验收标准：

- 单个组件不再承担上传、转换、编辑、导出全部职责。
- 代码结构能支撑后续编辑器功能扩展。

## 3. 编辑器核心功能

### [x] 3.1 实现可编辑网格画布

需求：

- 使用可交互网格展示图纸。
- 支持点击单个格子上色。
- 支持拖拽连续绘制。
- 支持空白格。
- 支持显示或隐藏网格线。

验收标准：

- 用户可以在空白画布上绘制图案。
- 网格在桌面和移动端都不溢出。
- 大小变化不会导致布局跳动。

### [x] 3.2 支持画笔、橡皮和填充

需求：

- 画笔：用当前选中颜色绘制。
- 橡皮：清空格子。
- 填充：填充相邻同色区域。
- 工具选择要清晰，适合触屏和鼠标操作。

验收标准：

- 三种基础工具可用。
- 填充不会卡死浏览器。
- 当前工具状态在 UI 上清楚可见。

### [x] 3.3 支持坐标编号和色号显示

需求：

- 顶部、左侧、右侧、底部显示坐标编号。
- 每个格子可显示颜色 code，例如 H2、E8、PB-01。
- 提供开关：显示色号 / 隐藏色号。
- 字号要适合不同 grid 尺寸，避免文字溢出。

验收标准：

- 生成的图纸接近用户上传参考图那种可制作样式。
- 坐标和色号在导出中也可复用。

### [x] 3.4 支持色卡和颜色统计

需求：

- 色卡面板显示颜色名称、色号、HEX。
- 用户可以选择当前画笔颜色。
- 颜色统计实时更新。
- 统计包括颜色、色号、数量、总用豆数。

验收标准：

- 编辑任意格子后统计同步更新。
- 统计数量总和等于已填充格子数。

### [x] 3.5 支持导出 PNG

需求：

- 导出当前编辑器图纸。
- 支持网格线、坐标编号、格子色号、底部颜色清单。
- 文件名包含 pinbead、尺寸和时间戳。

验收标准：

- 导出的 PNG 可直接用于制作参考。
- 导出内容与编辑器当前状态一致。

### [x] 3.6 重构 editor 为画板工作台布局

需求：

- 将 `/editor` 从普通页面工具布局升级为长期编辑更舒服的工作台布局。
- 中间区域以画板为主，画板放在独立 Canvas Stage 内，不再依赖页面横向滚动查看大画布。
- 左侧操作栏固定承载画布尺寸、视图开关和导出等画布级操作。
- 右侧操作栏固定承载当前颜色、色板搜索、色系筛选、已用颜色过滤和颜色统计。
- Brush、Eraser、Fill 等高频工具从左侧栏移出，改为像 Figma 一样的小型浮动工具 dock，悬浮在画板区域底部居中。
- 工具 dock 首版包含 Hand、Brush、Eraser、Fill，保留快捷键选择。
- Hand 工具支持用鼠标拖动画板位置，便于编辑 48x48、64x64 这类大图纸。
- 左侧工具栏支持折叠为窄 rail，减少画板编辑时的干扰。
- editor 不保留独立顶部导航栏，站点入口统一放进左侧功能区。
- 左右侧栏宽度需要克制，避免挤占画板主区域。
- 工作台外层不保留页面 padding 或列间 gap，侧栏与画板区域需要贴边。
- 画板支持 `Ctrl + 鼠标滚轮` 放大缩小，并尽量围绕鼠标位置缩放。
- 画板拖动需要使用适合高频交互的更新方式，避免每次鼠标移动都触发整块编辑器同步重渲染。
- 修改画布尺寸后需要自动回到居中视图。
- 站点滚动条统一优化为更细的样式，不显示上下按钮和滚动条角块。
- 保留原有格子绘制、拖拽绘制、橡皮擦、填充、色号显示、坐标显示、颜色统计和 PNG 导出能力。

验收标准：

- `/editor` 桌面端呈现左栏、画板、右栏三栏工作台结构。
- 画板工具 dock 位于画板区域内部底部居中，不占用侧栏。
- 选择 Hand 后可以拖动画板，不会误触上色或填充。
- Brush、Eraser、Fill 在新布局中仍可正常编辑格子。
- 48x48、64x64 画布可以通过拖动画板查看不同区域。
- 左侧栏可以折叠和展开。
- 左侧栏提供返回首页、导入图片和图库入口，折叠态也保留导航能力。
- `Ctrl + 鼠标滚轮` 可以缩放画板。
- `Ctrl + 鼠标滚轮` 不触发浏览器默认页面缩放。
- 工作台列之间没有明显空隙。
- Hand 拖动画板时移动响应要足够顺滑。
- `npm run lint` 和 `npm run build` 通过。

### [x] 3.7 将 editor 画板渲染迁移到 Canvas

需求：

- 将中间图纸区域从大量 DOM 格子按钮迁移为单个 `<canvas>` 渲染。
- React 继续负责左侧功能区、右侧色板统计和底部悬浮工具 dock。
- Pattern 数据模型保持不变，仍使用 `cells + colorId`，避免影响 `/convert` 和导出链路。
- Canvas 需要绘制格子颜色、网格线、坐标编号、格子色号和当前 hover 高亮。
- hover 高亮需要和主图纸绘制拆层，避免移动鼠标时重绘整张图纸。
- Canvas pointer 坐标需要准确换算到 row / column / cell index。
- Brush、Eraser、Fill 需要继续支持点击和拖拽绘制。
- 右键擦除、hover 状态、颜色统计和当前色号状态需要继续工作。
- Canvas 需要支持高清屏渲染，避免在 Retina 屏幕下明显发糊。
- PNG 导出仍复用现有导出逻辑，不依赖中间编辑画布截图。

验收标准：

- `/editor` 中间图纸不再渲染成上千个 button。
- 16x16、24x24、32x32、48x48、64x64 都能正常绘制。
- 打开或关闭网格线、坐标编号和色号后，Canvas 显示同步更新。
- Brush、Eraser、Fill 的点击和拖拽行为与迁移前一致。
- 鼠标移动到不同格子时，高亮反馈需要足够即时。
- `Ctrl + 鼠标滚轮` 缩放、Hand 拖动画板和 Canvas 绘制互不冲突。
- `npm run lint` 和 `npm run build` 通过。

## 4. 图片导入与编辑器联动

### [x] 4.1 `/convert` 生成可编辑草稿

需求：

- 上传图片后生成 Pattern 草稿。
- 草稿使用色卡颜色 ID。
- 用户可以调整尺寸、裁剪方式、颜色数量。
- 生成结果明确标记为 draft。

验收标准：

- 转换结果不是孤立预览，而是可传给编辑器的数据。

### [x] 4.2 从 `/convert` 打开到 `/editor`

需求：

- 用户点击 `Open in editor` 后进入 `/editor`。
- 编辑器加载转换生成的草稿。
- 第一版可以使用浏览器内存、URL state 或 localStorage 传递草稿。
- 不上传原图，不要求登录。

验收标准：

- 用户能在编辑器里修改转换结果。
- 刷新或离开页面时不默认保存用户原图。

## 5. 后台 CMS 与 SEO 内容页

### [x] 5.1 重做轻量首页

需求：

- 首页首屏展示品牌定位和 CTA。
- 主 CTA：`Start designing`。
- 次 CTA：`Convert image`、`Browse patterns`。
- 展示编辑器亮点，而不是完整编辑器。
- 首页末尾展示 12-20 个精选图纸瀑布流。

验收标准：

- 用户进入首页能马上理解 Pinbead。
- 首页能自然分流到 `/editor`、`/convert`、`/patterns`。
- 首页移动端首屏轻量。

### [x] 5.2 接入数据库和 Prisma

需求：

- 接入 PostgreSQL，作为后台 CMS、图纸库、分类、教程和发布状态的数据源。
- 接入 Prisma，建立迁移、类型和查询层。
- 建立 `AdminUser`、`Category`、`Pattern`、`Guide` 的第一版 schema。
- `Pattern` 使用 `cellsJson` 存储图纸矩阵，不把每个格子拆成数据库行。
- 保留 `draft`、`published`、`archived` 状态字段。
- 本地开发环境需要有清晰的 `.env` 示例和迁移命令说明。

验收标准：

- 本地可以执行 Prisma migrate。
- 应用可以连接数据库。
- 数据模型能支撑后台图纸发布流程。

### [x] 5.3 建立管理员登录

需求：

- 新建 `/admin/login`。
- 第一版只支持站长或管理员登录，不开放注册。
- 使用安全的密码哈希，不在代码中明文写密码。
- 登录后使用服务端 session 或签名 cookie。
- Cookie 必须设置 `httpOnly`、`secure`、`sameSite` 和过期时间。
- 登录接口需要基础限流，防止暴力尝试密码。
- 后台写操作需要 CSRF 防护或严格同源校验。
- 未登录访问 `/admin` 页面必须跳转到登录页。
- `/admin` 页面和后台 API 必须服务端鉴权，不能只靠前端隐藏按钮。
- `/admin` 相关页面需要 noindex，避免被搜索引擎收录。

验收标准：

- 管理员可以登录和退出。
- 未登录用户不能访问后台页面。
- 退出登录后当前 session 失效。
- 连续登录失败会被限制。
- 无有效登录态或 CSRF 校验的写请求会被拒绝。
- 后台页面不会进入公开 sitemap。

### [x] 5.4 建立后台图纸管理

需求：

- 新建 `/admin/patterns` 图纸列表。
- 支持按状态、分类、关键词筛选。
- 支持新建图纸 `/admin/patterns/new`。
- 支持编辑图纸 `/admin/patterns/[id]`。
- 图纸字段包括 title、slug、summary、description、category、difficulty、width、height、colorCount、beadCount、paletteId、cellsJson、previewImageUrl、downloadImageUrl、sourceType、seoTitle、seoDescription、status。
- 支持保存草稿、发布、下架。
- 发布前需要校验 slug、标题、分类、图纸数据和 SEO 基础字段。
- 支持上传预览图或下载文件时，文件必须写入持久化上传目录或对象存储，不写入 `.next`、临时目录或会被构建覆盖的目录。
- 上传文件需要限制类型、大小和图片尺寸，并使用随机文件名。

验收标准：

- 管理员可以创建和编辑图纸。
- 管理员可以把图纸从草稿发布为公开内容。
- 下架图纸不会在公开页面展示。
- 服务器重启或重新部署后，已上传预览图仍然可访问。
- 非白名单文件无法上传或公开访问。

### [x] 5.5 建立 `/patterns` 图纸库

需求：

- `/patterns` 从数据库读取 `published` 图纸。
- 展示精选原创图纸。
- 支持进入分类页。
- 支持进入单个图纸详情页。
- 页面需要有英文介绍文案，不能只是图纸列表。
- 页面需要自然链接到 `/editor` 和 `/convert`。

验收标准：

- `/patterns` 可被搜索引擎收录。
- 页面不是空列表，有后台发布的真实图纸内容。
- 未发布和下架图纸不会显示。

### [x] 5.6 建立 `/categories/[categorySlug]`

需求：

- 首批分类：Animals、Food、Holidays、Beginner、Cute、Nature、Letters & Numbers。
- 分类由后台管理。
- 每个分类有独特介绍、SEO title、SEO description。
- 每个分类至少有若干已发布图纸，不创建空分类。

验收标准：

- 分类页能承接长尾 SEO。
- 分类页到图纸详情页内链完整。
- 没有已发布内容的分类不进入公开导航和 sitemap。

### [ ] 5.7 建立 `/pattern/[patternSlug]`

需求：

- 单个图纸详情页从数据库读取 `published` 图纸。
- 页面包含预览、尺寸、难度、颜色数量、用豆统计、下载和制作提示。
- 页面包含 200-400 英文词的独立说明文案。
- 页面包含相关图纸。
- 页面 title 和 H1 面向英文 SEO。

验收标准：

- 图纸详情页不是薄页面。
- 用户可以下载或打开到编辑器继续编辑。
- 未发布或下架图纸返回 404。

### [ ] 5.8 建立 `/guides` 和教程详情页

需求：

- 教程列表页展示全部已发布 guide。
- 后台支持管理 guide 草稿和发布状态。
- 首批教程：
  - How to Make a Bead Pattern
  - How to Turn a Photo into a Bead Pattern
  - How to Use the Pinbead Editor
  - Bead Color Chart
  - How to Print and Follow a Bead Pattern
- 教程自然链接到编辑器、转换器和图纸库。

验收标准：

- 教程页能承接长尾 SEO。
- 教程不是纯文字堆砌，后续应加入截图或示例图纸。

### [ ] 5.9 支持从编辑器保存到后台草稿

需求：

- 该能力暂时不阻塞上线，放在 `/patterns`、分类页和详情页完成后再做。
- `/editor` 增加管理员可用的 `Save to library draft` 入口。
- 保存时把当前 Pattern 模型写入后台草稿。
- 草稿进入 `/admin/patterns/[id]` 后继续补充 SEO 和发布信息。
- 普通访客不显示后台保存入口。

验收标准：

- 管理员能从编辑器把图纸保存到后台。
- 保存后的图纸可以在后台继续编辑和发布。
- 不会上传或保存用户原始图片。

## 6. AdSense 和合规

### [ ] 6.1 建立基础合规页面

需求：

- Privacy Policy。
- Terms of Service。
- Contact。
- Copyright / DMCA。
- 隐私政策说明本地图片处理、Analytics、AdSense、cookies。

验收标准：

- 页脚能访问所有合规页面。
- 内容符合 Pinbead 实际业务。

### [ ] 6.2 AdSense 前站点体检

需求：

- 至少 30-50 个高质量图纸详情页。
- 至少 3-5 篇教程。
- 没有大量空白页、测试页、重复页。
- 广告不放在编辑器按钮、上传按钮、下载按钮附近。

验收标准：

- 站点达到可提交 AdSense 审核状态。

## 7. 后续用户系统和社区

### [ ] 7.1 判断是否启动用户系统

需求：

- 根据自然流量、编辑器使用、转换使用、用户反馈判断。
- 不在没有需求时过早做社区。

验收标准：

- 有数据支撑再进入登录、保存、分享功能。

### [ ] 7.2 用户保存和公开分享

需求：

- 登录用户可以保存图纸。
- 用户可以申请公开分享。
- 公开前必须确认权利和授权。
- 进入审核队列。
- 审核通过后才公开、索引和展示广告。

验收标准：

- 未审核内容不公开、不索引、不展示广告。
- 管理员可以下架违规图纸。
